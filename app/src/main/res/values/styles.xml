<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="NewTestTheme" parent="AppTheme">
        <item name="colorPrimaryDark">@android:color/holo_green_dark</item>
        <item name="colorPrimary">@android:color/holo_green_light</item>
        <item name="colorAccent">@android:color/holo_red_dark</item>
    </style>

    <style name="NewTestTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="NewTestTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="NewTestTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
